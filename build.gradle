subprojects {
    apply plugin: 'java'

    group 'com.vk.api'
    version '0.3.3-SNAPSHOT'

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}