subprojects {
    apply plugin: 'java'

    group 'com.vk.api'
    version '0.1.4-SNAPSHOT'

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}